#include<iostream>
#include<string>
using namespace std;
class Solution {
public:
    string minWindow(string s, string t) {
        if(s.length()<=0 || t.length()<=0 || t.length()>s.length())
        	return "";
        for(int i=0;i<s.length();i++)
        {
        	bool isVisit[s.length()];
		}
    }
    bool isInSting(char c,string s,bool isVisit)
    {
    	for(int i=0;i<s.length();i++)
    	{
    		
		}
	}
};
int main()
{
	string s="ADOBECODEBANC",t="ABC",ans=""; 
	return 0;
 } 
